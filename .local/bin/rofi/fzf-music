#!/bin/sh
cd ~/Music || exit
cd ~/Music/"$(ls -d */ | fzf)" || exit
#cd "$(ls -d * | fzf)" || exit
ls -d * | rofi -dmenu | xargs -I {} mpv --really-quiet "{}"
#mpv --really-quiet .

# mpv --really-quiet .
